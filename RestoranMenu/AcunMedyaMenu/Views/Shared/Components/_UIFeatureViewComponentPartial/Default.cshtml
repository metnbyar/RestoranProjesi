@model List<AcunMedyaMenu.Entities.Feature>

<div class="feature" id="feature">
    <div class="container">
        <div class="row">
            <!-- Sol Kısım: Özellikler Resimleri ve Tanıtım Metni -->
            <div class="col-lg-5">
                <div class="section-header">
                    <p>Neden Bizi Seçmelisiniz</p>
                    <h2>Öne Çıkan Özelliklerimiz</h2>
                </div>
                <div class="feature-text">
                    <div class="feature-img">
                        <div class="row">
                            @for (int i = 0; i < 4; i++)
                            {
                                <div class="col-6 mb-4">
                                    <img src="@((i < Model?.Count && !string.IsNullOrEmpty(Model[i].ImageUrl)) ? Model[i].ImageUrl : "~/images/default-image.jpg")"
                                         alt="@((i < Model?.Count && !string.IsNullOrEmpty(Model[i].Title)) ? Model[i].Title : "Default Image")"
                                         class="img-fluid">
                                </div>
                            }
                        </div>
                    </div>
                    <p>
                        Restoranımız, eşsiz lezzetler ve yüksek kaliteli malzemelerle hazırlanan yemekler sunmaktadır. Uzman şeflerimiz ve hızlı servisimiz ile misafirlerimize en iyi deneyimi yaşatmayı hedefliyoruz.
                    </p>
                    <a class="btn custom-btn" href="#booking">Masa Rezervasyonu</a>
                </div>
            </div>

            <!-- Sağ Kısım: Özellikler Listesi -->
            <div class="col-lg-7">
                <div class="row">
                    @foreach (var feature in Model)
                    {
                        <div class="col-sm-6 mb-4">
                            <div class="feature-item">
                                <i class="@feature.IconClass"></i>
                                <h3>@feature.Title</h3>
                                <p>@feature.Description</p>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
